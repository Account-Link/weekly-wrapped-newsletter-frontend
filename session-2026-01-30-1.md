## 核心上下文摘要

- GET /api/wrapped 默认走真实接口，传 mock=true 或 case 参数走本地 mock
- Firebase Admin 默认关闭，仅在 FIREBASE_ADMIN_ENABLED=true 时初始化
- WeeklyData 新增 feedlingState 字段用于模板分状态渲染
- 开场小猫图与文案根据 feedlingState 映射，文案高亮关键短语
